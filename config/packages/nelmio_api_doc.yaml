nelmio_api_doc:
    documentation:
        info:
            title: KnowShare API
            description: API documentation for KnowShare application
            version: 1.0.0
        components:
            securitySchemes:
                bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
                    description: 'JWT token obtained from login endpoint'
    areas: # to filter documented areas
        path_patterns:
            - ^/api/v1
