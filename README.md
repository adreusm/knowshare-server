# KnowShare API Server

REST API сервер для платформы обмена знаниями. Позволяет пользователям создавать заметки, организовывать их по доменам (предметным областям), использовать теги и подписываться на других авторов.

## Технологии

- **PHP 8.2+**
- **Symfony 7.3**
- **PostgreSQL 17**
- **Doctrine ORM 3.5**
- **JWT Authentication** (Lexik JWT Bundle)
- **OpenAPI/Swagger** (Nelmio API Doc Bundle)

## Основные возможности

- 🔐 JWT аутентификация с refresh токенами
- 📝 Управление заметками с уровнями доступа (public, subscribers, private)
- 📚 Организация заметок по доменам (предметным областям)
- 🏷️ Система тегов для категоризации
- 👥 Подписки на авторов
- 📄 Пагинация для всех списков
- 📖 OpenAPI документация

## Требования

- PHP 8.2 или выше
- PostgreSQL 17
- Composer
- Docker и Docker Compose (опционально)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd knowshare-server
```

2. Установите зависимости:
```bash
composer install
```

3. Настройте переменные окружения:
```bash
cp .env .env.local
```

Отредактируйте `.env.local` и укажите:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `JWT_SECRET_KEY` - путь к приватному ключу JWT
- `JWT_PUBLIC_KEY` - путь к публичному ключу JWT
- `JWT_PASSPHRASE` - пароль для JWT ключей

4. Запустите базу данных (через Docker):
```bash
docker-compose up -d
```

5. Выполните миграции:
```bash
php bin/console doctrine:migrations:migrate
```

6. Сгенерируйте JWT ключи (если еще не созданы):
```bash
php bin/console lexik:jwt:generate-keypair
```

## Запуск

### Development сервер:
```bash
symfony server:start
```

Или через встроенный PHP сервер:
```bash
php -S localhost:8000 -t public
```

API будет доступен по адресу: `http://localhost:8000/api/v1`


